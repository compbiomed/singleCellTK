---
title: "tSNE/UMAP using Seurat"
date: "`r format(Sys.time(), '%B %d, %Y')`"
params:
  object: object
output:
  html_document:
    number_sections: yes
    toc: yes
    code_folding: hide
---
```{r}
if(dr == "umap"){
  data <- seuratRunUMAP(data, useReduction = "pca", dims = significant_PC, verbose = FALSE)
}else{
  
}
```