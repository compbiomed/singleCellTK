---
title: "Reduction Plot using Seurat"
date: "`r format(Sys.time(), '%B %d, %Y')`"
params:
  object: object
output:
  html_document:
    number_sections: yes
    toc: yes
    code_folding: hide
---
```{r}
plot <- seuratReductionPlot(data, useReduction = useReduction, showLegend = showLegend, groupBy = groupBy, splitBy = splitBy)
plot
```